function diff(a: string, b: string) {
	const n = a.length;
	const m = b.length;
	const max = m + n;
	const v = new Array(max);
	v.fill(-1);
	for (let d = 1; d < v.length; d++) {
		for (let k = -d; k <= d; d = d + 2) {

		}
	}
}